---
// SLi Project - Final dengan Popup Chat AI dan n8n Webhook
---

<html lang="id">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<title>SLi Project | AI & Digital Solutions</title>
		<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;700;800&display=swap" rel="stylesheet">
		
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
	</head>
	<body>
		<main>
			<section class="hero">
				<div class="glow"></div>
				<div class="content">
					<div class="badge">Future Technology</div>
					<h1>SLi <span class="blue-text">Project</span></h1>
					<p class="tagline">Digitalisasi & Otomatisasi AI untuk membantu segala kesulitan perusahaan Anda.</p>
					
					<div class="btn-group">
						<a href="https://wa.me/6285172076698" class="btn-wa" target="_blank">
							<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l2.27-2.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path></svg>
							Hubungi WhatsApp
						</a>
						<a href="mailto:sliproject.id@gmail.com" class="btn-email">
							<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path><polyline points="22,6 12,13 2,6"></polyline></svg>
							Kirim Email
						</a>
					</div>
				</div>
			</section>

			<section id="services" class="services">
				<div class="grid">
					<div class="card">
						<div class="icon-box">
							<svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#3b82f6" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 8V4H8"></path><rect width="16" height="12" x="4" y="8" rx="2"></rect><path d="M2 14h2"></path><path d="M20 14h2"></path><path d="M15 13v2"></path><path d="M9 13v2"></path></svg>
						</div>
						<h3>Automation AI</h3>
						<p>AI Sales, Manajemen, HRD, dan Admin Sosmed.</p>
					</div>
					<div class="card">
						<div class="icon-box">
							<svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#3b82f6" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="16 18 22 12 16 6"></polyline><polyline points="8 6 2 12 8 18"></polyline></svg>
						</div>
						<h3>Web & App Dev</h3>
						<p>Pembuatan Website & Aplikasi custom.</p>
					</div>
				</div>
			</section>

			<button id="openChatBtn" class="admin-float">
				<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path></svg>
				<span>Hubungi Admin</span>
			</button>

			<div id="chatPopup" class="chat-popup">
				<div class="chat-header">
					<h3>SLi Chatbot AI</h3>
					<button id="closeChatBtn" class="close-btn">&times;</button>
				</div>
				<div class="chat-body" id="chatBody">
					<div class="message bot-message">Halo! Saya SLi Chatbot. Ada yang bisa saya bantu?</div>
				</div>
				<div class="chat-footer">
					<input type="text" id="chatInput" placeholder="Ketik pesan Anda...">
					<button id="sendMessageBtn"><i class="fas fa-paper-plane"></i></button>
				</div>
			</div>
		</main>

		<script>
			const openChatBtn = document.getElementById('openChatBtn');
			const closeChatBtn = document.getElementById('closeChatBtn');
			const chatPopup = document.getElementById('chatPopup');
			const chatInput = document.getElementById('chatInput');
			const sendMessageBtn = document.getElementById('sendMessageBtn');
			const chatBody = document.getElementById('chatBody');

			// Buka/tutup popup chat
			openChatBtn.addEventListener('click', () => {
				chatPopup.classList.add('active');
			});

			closeChatBtn.addEventListener('click', () => {
				chatPopup.classList.remove('active');
			});

			// Fungsi untuk mengirim pesan
			async function sendMessage() {
				const userMessage = chatInput.value.trim();
				if (userMessage === '') return;

				// Tampilkan pesan pengguna
				const userMsgDiv = document.createElement('div');
				userMsgDiv.classList.add('message', 'user-message');
				userMsgDiv.textContent = userMessage;
				chatBody.appendChild(userMsgDiv);
				chatInput.value = '';
				chatBody.scrollTop = chatBody.scrollHeight; // Scroll ke bawah

				// Kirim pesan ke webhook n8n
				try {
					const response = await fetch('https://n8n.aleshatour.my.id/webhook/botwebsite', {
						method: 'POST',
						headers: {
							'Content-Type': 'application/json',
						},
						body: JSON.stringify({ message: userMessage }),
					});
					const data = await response.json();

					// Tampilkan balasan dari n8n (contoh: balasan dari AI Anda)
					const botMsgDiv = document.createElement('div');
					botMsgDiv.classList.add('message', 'bot-message');
					// Pastikan n8n Anda mengembalikan JSON dengan key 'reply' atau 'message'
					botMsgDiv.textContent = data.reply || data.message || "Maaf, saya tidak mengerti."; 
					chatBody.appendChild(botMsgDiv);
					chatBody.scrollTop = chatBody.scrollHeight; // Scroll ke bawah

				} catch (error) {
					console.error('Error sending message to n8n:', error);
					const errorMsgDiv = document.createElement('div');
					errorMsgDiv.classList.add('message', 'bot-message', 'error-message');
					errorMsgDiv.textContent = 'Terjadi kesalahan. Silakan coba lagi.';
					chatBody.appendChild(errorMsgDiv);
					chatBody.scrollTop = chatBody.scrollHeight;
				}
			}

			sendMessageBtn.addEventListener('click', sendMessage);
			chatInput.addEventListener('keypress', (e) => {
				if (e.key === 'Enter') {
					sendMessage();
				}
			});
		</script>
	</body>
</html>

<style>
	:root { --bg: #020617; --primary: #3b82f6; --text: #f8fafc; --chat-bg: #1f2937; --chat-bubble-bot: #4a5568; --chat-bubble-user: #3b82f6; }
	* { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Plus Jakarta Sans', sans-serif; }
	body { background-color: var(--bg); color: var(--text); }
	.hero { height: 100vh; display: flex; align-items: center; justify-content: center; text-align: center; background: radial-gradient(circle at 50% 50%, #1e293b 0%, #020617 100%); }
	.blue-text { color: var(--primary); text-shadow: 0 0 30px rgba(59, 130, 246, 0.5); }
	.btn-group { display: flex; gap: 16px; justify-content: center; }
	.btn-wa { background: #22c55e; color: white; padding: 16px 32px; border-radius: 12px; text-decoration: none; font-weight: bold; display: flex; align-items: center; gap: 10px; transition: 0.3s; }
	.btn-email { background: white; color: black; padding: 16px 32px; border-radius: 12px; text-decoration: none; font-weight: bold; display: flex; align-items: center; gap: 10px; transition: 0.3s; }
	.btn-wa:hover, .btn-email:hover { transform: translateY(-5px); box-shadow: 0 10px 20px rgba(0,0,0,0.3); }
	.services { padding: 50px 24px; max-width: 1200px; margin: 0 auto; }
	.grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 32px; }
	.card { background: #0f172a; padding: 40px; border-radius: 24px; text-align: center; border: 1px solid rgba(255, 255, 255, 0.05); }
	.icon-box { margin-bottom: 20px; display: flex; justify-content: center; }
	
	/* Style untuk Tombol Floating Admin */
	.admin-float {
		position: fixed;
		bottom: 30px;
		right: 30px;
		background: var(--primary);
		padding: 12px 24px;
		border-radius: 50px;
		display: flex;
		align-items: center;
		gap: 10px;
		color: white;
		border: none;
		cursor: pointer;
		font-weight: bold;
		box-shadow: 0 4px 15px rgba(59, 130, 246, 0.4);
		z-index: 1000;
		transition: 0.3s;
	}
	.admin-float:hover { transform: translateY(-5px); background: #2563eb; }

	@media (max-width: 480px) {
		.admin-float span { display: none; }
		.admin-float { padding: 15px; border-radius: 50%; }
	}

	/* Style untuk Popup Chatbot */
	.chat-popup {
		position: fixed;
		bottom: 90px; /* Di atas tombol admin-float */
		right: 30px;
		width: 350px;
		height: 450px;
		background-color: var(--chat-bg);
		border-radius: 15px;
		box-shadow: 0 10px 40px rgba(0, 0, 0, 0.4);
		display: flex;
		flex-direction: column;
		overflow: hidden;
		z-index: 999;
		transform: scale(0);
		transform-origin: bottom right;
		transition: transform 0.3s ease-out;
		border: 1px solid #334155;
	}
	.chat-popup.active {
		transform: scale(1);
	}
	.chat-header {
		background-color: var(--primary);
		color: white;
		padding: 15px;
		display: flex;
		justify-content: space-between;
		align-items: center;
		font-weight: bold;
	}
	.chat-header h3 { margin: 0; font-size: 1.1rem; }
	.close-btn {
		background: none;
		border: none;
		color: white;
		font-size: 1.5rem;
		cursor: pointer;
		line-height: 1;
	}
	.chat-body {
		flex-grow: 1;
		padding: 15px;
		overflow-y: auto;
		background-color: #2a3547;
		display: flex;
		flex-direction: column;
		gap: 10px;
	}
	.message {
		max-width: 80%;
		padding: 10px 15px;
		border-radius: 15px;
		font-size: 0.95rem;
		line-height: 1.5;
	}
	.bot-message {
		background-color: var(--chat-bubble-bot);
		color: white;
		align-self: flex-start;
		border-bottom-left-radius: 5px;
	}
	.user-message {
		background-color: var(--chat-bubble-user);
		color: white;
		align-self: flex-end;
		border-bottom-right-radius: 5px;
	}
	.chat-footer {
		display: flex;
		padding: 10px;
		background-color: var(--chat-bg);
		border-top: 1px solid #334155;
	}
	.chat-footer input {
		flex-grow: 1;
		padding: 10px;
		border: 1px solid #4a5568;
		border-radius: 20px;
		background-color: #1a202c;
		color: white;
		font-size: 0.95rem;
		outline: none;
	}
	.chat-footer input::placeholder { color: #a0aec0; }
	.chat-footer button {
		background-color: var(--primary);
		color: white;
		border: none;
		border-radius: 50%;
		width: 40px;
		height: 40px;
		display: flex;
		justify-content: center;
		align-items: center;
		margin-left: 10px;
		cursor: pointer;
		transition: background-color 0.2s;
	}
	.chat-footer button:hover { background-color: #2563eb; }

	/* Responsif untuk ukuran layar kecil */
	@media (max-width: 600px) {
		.chat-popup {
			width: calc(100% - 60px); /* Lebar penuh minus padding */
			right: 30px;
			left: 30px;
			bottom: 90px;
			height: calc(100% - 120px); /* Hampir penuh layar */
		}
	}
</style>