---
// SLi Project - Final Pro Fix Jarak & Chatbot
---

<html lang="id">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<title>SLi Project | AI & Digital Solutions</title>
		<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;700;800&display=swap" rel="stylesheet">
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
	</head>
	<body>
		<main>
			<section class="hero">
				<div class="glow"></div>
				<div class="content">
					<div class="badge">Future Technology</div>
					<h1>SLi <span class="blue-text">Project</span></h1>
					<p class="tagline">Digitalisasi & Otomatisasi AI untuk membantu segala kesulitan perusahaan Anda.</p>
					
					<div class="btn-group">
						<a href="https://wa.me/6285172076698" class="btn-wa" target="_blank">
							<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l2.27-2.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path></svg>
							Hubungi WhatsApp
						</a>
						<a href="mailto:sliproject.id@gmail.com" class="btn-email">
							<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path><polyline points="22,6 12,13 2,6"></polyline></svg>
							Kirim Email
						</a>
					</div>
				</div>
			</section>

			<section id="services" class="services">
				<div class="grid">
					<div class="card">
						<div class="icon-box">
							<svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#3b82f6" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 8V4H8"></path><rect width="16" height="12" x="4" y="8" rx="2"></rect><path d="M2 14h2"></path><path d="M20 14h2"></path><path d="M15 13v2"></path><path d="M9 13v2"></path></svg>
						</div>
						<h3>Automation AI</h3>
						<p>AI Sales, Manajemen, HRD, dan Admin Sosmed.</p>
					</div>
					<div class="card">
						<div class="icon-box">
							<svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#3b82f6" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="16 18 22 12 16 6"></polyline><polyline points="8 6 2 12 8 18"></polyline></svg>
						</div>
						<h3>Web & App Dev</h3>
						<p>Pembuatan Website & Aplikasi custom.</p>
					</div>
				</div>
			</section>

			<button id="openChatBtn" class="admin-float">
				<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path></svg>
				<span>Hubungi Admin</span>
			</button>

			<div id="chatPopup" class="chat-popup">
				<div class="chat-header">
					<h3>SLi Chatbot AI</h3>
					<button id="closeChatBtn" class="close-btn">&times;</button>
				</div>
				<div class="chat-body" id="chatBody">
					<div class="message bot-message">Halo! Ada yang bisa saya bantu?</div>
				</div>
				<form class="chat-footer" id="chatForm">
					<input type="text" id="chatInput" placeholder="Ketik pesan..." autocomplete="off">
					<button type="submit" id="sendMessageBtn"><i class="fas fa-paper-plane"></i></button>
				</form>
			</div>
		</main>

		<script>
			const openChatBtn = document.getElementById('openChatBtn');
			const closeChatBtn = document.getElementById('closeChatBtn');
			const chatPopup = document.getElementById('chatPopup');
			const chatForm = document.getElementById('chatForm');
			const chatInput = document.getElementById('chatInput');
			const chatBody = document.getElementById('chatBody');

			openChatBtn.addEventListener('click', () => chatPopup.classList.add('active'));
			closeChatBtn.addEventListener('click', () => chatPopup.classList.remove('active'));

			chatForm.addEventListener('submit', async (e) => {
				e.preventDefault();
				const msg = chatInput.value.trim();
				if (!msg) return;

				// User Message
				appendMessage(msg, 'user-message');
				chatInput.value = '';

				try {
					const response = await fetch('https://n8n.aleshatour.my.id/webhook/botwebsite', {
						method: 'POST',
						headers: { 'Content-Type': 'application/json' },
						body: JSON.stringify({ message: msg }),
					});
					
					if(!response.ok) throw new Error('Network response was not ok');
					
					const data = await response.json();
					appendMessage(data.reply || data.message || "Pesan terkirim ke sistem.", 'bot-message');
				} catch (error) {
					appendMessage("Maaf, koneksi ke asisten AI terputus.", 'bot-message');
				}
			});

			function appendMessage(text, className) {
				const div = document.createElement('div');
				div.classList.add('message', className);
				div.textContent = text;
				chatBody.appendChild(div);
				chatBody.scrollTop = chatBody.scrollHeight;
			}
		</script>
	</body>
</html>

<style>
	:root { --bg: #020617; --primary: #3b82f6; --text: #f8fafc; --chat-bg: #1f2937; }
	* { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Plus Jakarta Sans', sans-serif; }
	body { background-color: var(--bg); color: var(--text); overflow-x: hidden; }
	
	.hero { min-height: 90vh; display: flex; align-items: center; justify-content: center; text-align: center; padding: 60px 20px; }
	
	/* JARAK TAGLINE DIPERBAIKI */
	.tagline { 
		color: #94a3b8; 
		max-width: 600px; 
		margin: 35px auto 60px; /* Jarak atas 35px, jarak bawah 60px agar tidak mepet tombol */
		line-height: 1.8; 
		font-size: 1.1rem; 
	}

	.btn-group { display: flex; gap: 20px; justify-content: center; flex-wrap: wrap; margin-top: 20px; }
	.blue-text { color: var(--primary); text-shadow: 0 0 30px rgba(59, 130, 246, 0.5); }
	
	.btn-wa { background: #22c55e; color: white; padding: 16px 32px; border-radius: 12px; text-decoration: none; font-weight: bold; display: flex; align-items: center; gap: 10px; transition: 0.3s; }
	.btn-email { background: white; color: black; padding: 16px 32px; border-radius: 12px; text-decoration: none; font-weight: bold; display: flex; align-items: center; gap: 10px; transition: 0.3s; }
	.btn-wa:hover, .btn-email:hover { transform: translateY(-5px); }

	.services { padding: 80px 24px; max-width: 1100px; margin: 0 auto; }
	.grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 32px; }
	.card { background: #0f172a; padding: 40px; border-radius: 24px; border: 1px solid rgba(255, 255, 255, 0.05); text-align: center; }

	/* POPUP CHAT */
	.admin-float { position: fixed; bottom: 30px; right: 30px; background: var(--primary); padding: 12px 24px; border-radius: 50px; color: white; border: none; cursor: pointer; font-weight: bold; z-index: 1000; display: flex; align-items: center; gap: 10px; box-shadow: 0 4px 15px rgba(59, 130, 246, 0.4); }
	.chat-popup { position: fixed; bottom: 95px; right: 30px; width: 340px; height: 450px; background: var(--chat-bg); border-radius: 15px; display: flex; flex-direction: column; transform: scale(0); transform-origin: bottom right; transition: 0.3s; z-index: 999; border: 1px solid #334155; box-shadow: 0 10px 40px rgba(0,0,0,0.5); }
	.chat-popup.active { transform: scale(1); }
	.chat-header { background: var(--primary); padding: 15px; display: flex; justify-content: space-between; align-items: center; }
	.chat-body { flex: 1; padding: 15px; overflow-y: auto; display: flex; flex-direction: column; gap: 10px; background: #111827; }
	.message { padding: 10px 14px; border-radius: 12px; max-width: 80%; font-size: 0.9rem; line-height: 1.4; }
	.bot-message { background: #374151; color: white; align-self: flex-start; }
	.user-message { background: var(--primary); color: white; align-self: flex-end; }
	.chat-footer { padding: 10px; display: flex; gap: 8px; border-top: 1px solid #334155; }
	.chat-footer input { flex: 1; padding: 10px; border-radius: 8px; border: none; background: #0f172a; color: white; outline: none; }
	.chat-footer button { background: var(--primary); border: none; color: white; width: 40px; height: 40px; border-radius: 8px; cursor: pointer; }

	@media (max-width: 480px) {
		.admin-float span { display: none; }
		.chat-popup { width: calc(100% - 40px); bottom: 85px; right: 20px; }
	}
</style>